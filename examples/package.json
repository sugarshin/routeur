{
  "name": "examples",
  "description": "",
  "scripts": {
    "watch:js": "watchify -d src/index.js -t babelify -o bundle.js -v",
    "serve": "browser-sync start --files ./bundle.js --server",
    "start": "npm run watch:js & npm run serve"
  },
  "license": "MIT",
  "devDependencies": {
    "babelify": "^6.3.0",
    "browser-sync": "^2.9.3",
    "watchify": "^3.4.0"
  },
  "dependencies": {
    "router": "file:../"
  }
}
